---
apiVersion: v1
kind: Service
metadata:
  name: &app truenas
  namespace: networking
  labels:
    app.kubernetes.io/name: *app
    app.kubernetes.io/instance: *app
spec:
  type: ExternalName
  externalName: "10.0.5.3"
  ports:
    - name: https
      port: &port 80
      protocol: TCP
      targetPort: *port
